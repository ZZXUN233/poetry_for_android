<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zhang.poemocean">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:testOnly="false"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.activity_login"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--<meta-data-->
        <!--android:name="preloaded_fonts"-->
        <!--android:resource="@array/preloaded_fonts" />-->
        <activity android:name=".MainActivity" />
        <activity android:name=".test.test_ui_show" />
        <activity android:name=".activity.activity_poemList" />
        <activity android:name=".activity.activity_poem_detail" />
        <activity android:name=".activity.activity_poet_info" />
        <activity android:name=".activity.activity_collections" />
        <activity android:name=".person_page.Homepage" />
        <activity android:name=".person_page.Publish" />
        <activity android:name=".activity.activity_setting" />
        <activity android:name=".activity.activity_comments" />
        <activity android:name=".activity.activity_register" />
    </application>

</manifest>